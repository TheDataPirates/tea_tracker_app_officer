import 'package:flutter/material.dart';

class RollingOutputScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rolling Output'),
      ),
    );
  }
}
