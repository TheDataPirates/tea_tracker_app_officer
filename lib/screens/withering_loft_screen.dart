import 'package:flutter/material.dart';

class WitheringLoftScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Withering Loft'),
      ),
    );
  }
}
