import 'package:flutter/material.dart';

class DifferenceReportScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Difference Report'),
      ),
    );
  }
}
