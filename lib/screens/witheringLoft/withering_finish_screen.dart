import 'package:flutter/material.dart';

class WitheringFinishScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Withering Finish'),
      ),
    );
  }
}
