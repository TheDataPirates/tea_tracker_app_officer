import 'package:flutter/material.dart';

class WitheringMixingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Withering Mixing'),
      ),
    );
  }
}
