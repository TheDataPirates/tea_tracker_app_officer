import 'package:flutter/material.dart';

class TroughUnloadingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Trough Unloading'),
      ),
    );
  }
}
